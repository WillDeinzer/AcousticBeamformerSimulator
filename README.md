## Running the simulation
1. Create a virtual environment if desired with `python -m venv <name>`
2. In the terminal, run `pip install requirements.txt`
3. Use the command `python gui.py` to start the simulation